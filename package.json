{
  "name": "updated-auto-refresh",
  "version": "1.0.0",
  "description": "Refresh the page when the page is updated",
  "module": "./lib/bundle.js",
  "types": "./lib/index.d.ts",
  "files": ["lib"],
  "scripts": {
    "build": "tsc --declaration --emitDeclarationOnly  && node esbuild.config.js"
  },
  "keywords": ["auto-refresh"],
  "author": "fancy_fish",
  "license": "ISC",
  "dependencies": {
    "esbuild": "0.17.19"
  },
  "devDependencies": {
    "@mistjs/eslint-config-vue": "^0.0.5"
  }
}
